// This file is used in Vue.js projects to configure a tool called Babel. 
// Babel is for converting modern JavaScript code into a version that is compatible with older web browsers.

module.exports = 
{
  presets: [
    '@vue/cli-plugin-babel/preset'
  ]
}